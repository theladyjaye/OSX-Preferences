force_color_prompt=yes
alias ls='ls --color=auto'

if [ -f "$HOME/.pythonrc" ]; then
    . "$HOME/.pythonrc"
fi

if [ -f "$HOME/.miscrc" ]; then
    . "$HOME/.miscrc"
fi

if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi
